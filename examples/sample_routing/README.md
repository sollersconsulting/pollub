You've to install Node.js. Then run
- `npm install -g grunt-cli`
- `npm install -g bower`
from the command line and run
- `npm install`
- `bower install`
from the command line in app directory.
Run `grunt` to start server.
